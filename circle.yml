machine:
  ruby:
    version: 2.0.0-p0

## Customize dependencies
dependencies:
  override:
    - bundle
    - bundle exec rake appraisal:install

## Customize test commands
test:
  override:
    - bundle exec rake
